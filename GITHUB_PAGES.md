# تشغيل Sky ERP على GitHub Pages

تمت إضافة workflow تلقائي في `.github/workflows/deploy-pages.yml` لبناء الواجهة الأمامية ونشرها على GitHub Pages عند كل push إلى الفرع `main`.

## التفعيل

من مستودع GitHub افتح **Settings → Pages**، ثم اختر **GitHub Actions** كمصدر النشر. بعد ذلك ادفع التغييرات إلى `main` أو شغّل workflow يدويًا من تبويب **Actions**. رابط الموقع المتوقع هو:

`https://engaccrashadbadr.github.io/Sky.ERP/`

يستخدم البناء قيمة `VITE_BASE_PATH=/Sky.ERP/`، ويضيف نسخة `404.html` من الصفحة الرئيسية حتى لا تفشل واجهة SPA عند إعادة تحميل الصفحة.

## حدود GitHub Pages

GitHub Pages يستضيف ملفات الواجهة الثابتة فقط. أما تسجيل الدخول عبر OAuth، وواجهات tRPC، وقاعدة بيانات MySQL، والتقارير الحية، وعمليات الإدخال والتصدير المرتبطة بالخادم، فتحتاج إلى تشغيل الخادم الحالي على استضافة خلفية مثل Manus WebDev أو خدمة Node متوافقة. لذلك فإن نشر الواجهة على GitHub Pages لا يحول النظام إلى ERP مستقل بلا خادم.

للحصول على نظام Sky ERP كامل الوظائف، استخدم رابط الواجهة المنشور مع خادم API عام مضبوط في العميل، أو استمر باستخدام نشر Manus الذي يشغل الواجهة والخادم وقاعدة البيانات معًا. لا تُضع أسرار البيئة أو مفاتيح OAuth داخل مستودع GitHub أو داخل ملفات الواجهة.
