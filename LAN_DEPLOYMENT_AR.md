# تشغيل Sky ERP على شبكة محلية

## البنية الموصى بها

يعمل Sky ERP داخل الشبكة المحلية من خلال جهاز مركزي واحد يسمى **خادم النظام**. هذا الجهاز يشغّل Backend وواجهة Sky ERP ويتصل بقاعدة البيانات المشتركة. أجهزة المستخدمين لا تحتاج إلى قاعدة بيانات مستقلة؛ تفتح الواجهة من متصفح داخل نفس الشبكة باستخدام عنوان الخادم.

| المكوّن | مكان التشغيل | الوظيفة |
|---|---|---|
| Sky ERP Backend | جهاز خادم ثابت داخل الشركة | API، المصادقة، التقارير، الحفظ، وواجهة النظام |
| قاعدة البيانات | MySQL/TiDB متاحة للخادم | حفظ بيانات الشركة مركزيًا |
| أجهزة الموظفين | أي جهاز داخل الشبكة | فتح `http://SERVER-LAN-IP:3000/` من المتصفح |

## تشغيل الخادم على Windows

ثبّت Node.js إصدار LTS وpnpm على جهاز الخادم، ثم انسخ المشروع كاملًا إلى مجلد ثابت. افتح PowerShell داخل مجلد المشروع وشغّل:

```powershell
pnpm install
pnpm run build
$env:HOST="0.0.0.0"
$env:PORT="3000"
$env:NODE_ENV="production"
pnpm run start:lan
```

يمكن بدلًا من ذلك تشغيل الملف `start-sky-erp-lan.bat` بالنقر المزدوج عليه. يظل نافذة الخادم مفتوحة أثناء استخدام النظام.

## فتح النظام من أجهزة الشبكة

على جهاز الخادم نفّذ `ipconfig` وابحث عن عنوان IPv4 في كارت الشبكة، مثل `192.168.1.25`. من أي جهاز آخر على نفس الشبكة افتح:

```text
http://192.168.1.25:3000/
```

إذا تغير عنوان الخادم، استخدم حجز DHCP في جهاز الراوتر أو اضبط عنوانًا ثابتًا للخادم.

## إعداد جدار حماية Windows

يجب السماح بالاتصالات الواردة إلى منفذ `3000` على جهاز الخادم من الشبكة الخاصة فقط. من PowerShell بصلاحيات المسؤول:

```powershell
New-NetFirewallRule -DisplayName "Sky ERP LAN 3000" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow -Profile Private
```

لا تفتح المنفذ على ملف تعريف Public، ولا تعمل Port Forwarding من الإنترنت إلى جهاز الشركة إلا بعد إعداد VPN وحماية مناسبة.

## قاعدة البيانات والبيانات السرية

ضع `DATABASE_URL` و`JWT_SECRET` وباقي المتغيرات المطلوبة في بيئة جهاز الخادم فقط، وليس داخل الواجهة أو ملفات GitHub. يجب أن تكون قاعدة البيانات قابلة للوصول من جهاز الخادم، مع إنشاء مستخدم قاعدة بيانات بصلاحيات محدودة.

## ملاحظات مهمة عن الإصدار الحالي

هذا الإعداد يشغّل التطبيق كخادم شبكة محلية واحد، وهو أفضل من تثبيت قاعدة بيانات منفصلة على كل جهاز. يلزم أن يكون جهاز الخادم قيد التشغيل حتى يستطيع المستخدمون الوصول إلى النظام. كما أن OAuth والتخزين الخارجيين الموجودين في النسخة الحالية قد يحتاجان إلى اتصال إنترنت؛ تشغيل النظام دون إنترنت بالكامل يتطلب استبدال المصادقة والتخزين بخدمات محلية.

لتحويل الخادم إلى ملف Windows `.exe` مستقل لاحقًا، يمكن تغليف Node.js وواجهة التطبيق باستخدام Electron، لكن قاعدة البيانات ستظل مشتركة على الخادم أو ستحتاج إلى SQLite محلية مع تصميم مختلف. ملف `.bat` المرفق هو طريقة التشغيل الموثوقة الحالية لأنه لا ينسخ أسرار الاتصال داخل برنامج قابل للفك.

## ملف Windows التنفيذي

أضيفت بنية Electron وملف بناء باسم `desktop:build`. عند تشغيله على Windows ينتج مثبتًا باسم `Sky-ERP-Setup-<version>.exe` داخل مجلد `release`. المثبت يفتح نافذة سطح مكتب ويشغّل خادم Sky ERP المحلي، ويمكن استخدامه على جهاز الخادم المركزي.

لأن بيئة التطوير الحالية Linux، يتم إنتاج ملف Windows تلقائيًا من GitHub Actions عبر الملف `.github/workflows/build-windows.yml`. من تبويب **Actions** شغّل workflow باسم **Build Sky ERP Windows installer**، ثم حمّل artifact باسم `sky-erp-windows-installer`.

في بنية الشركة المقترحة يُثبّت ملف `.exe` على جهاز الخادم فقط، وتفتح أجهزة الموظفين عنوان الخادم من المتصفح. تشغيل المثبت على كل جهاز سيؤدي إلى إنشاء خادم مستقل وقاعدة بيانات مستقلة ما لم تُضبط جميع الأجهزة على Backend وقاعدة بيانات مشتركة، لذلك لا يُنصح به لبيانات ERP مشتركة.

## نمط Server EXE وClient EXE الاختياري

تمت إضافة نمط اختياري دون إلغاء المثبت السابق. استخدم `Sky-ERP-Server-Setup-<version>.exe` على جهاز مركزي واحد فقط؛ فهو يشغّل الخادم المشترك وقاعدة البيانات المرتبطة به. استخدم `Sky-ERP-Client-Setup-<version>.exe` على أجهزة الموظفين؛ فهو لا ينشئ قاعدة بيانات محلية، بل يفتح اتصالًا بواجهة الخادم المركزي.

بعد تثبيت Client EXE، أنشئ الملف `client-config.json` داخل مجلد إعدادات التطبيق في Windows، وضع عنوان الخادم المركزي، مثل:

```json
{
  "serverUrl": "http://192.168.1.25:3000"
}
```

يمكن أيضًا تمرير العنوان مؤقتًا بالوسيط `--server-url=http://192.168.1.25:3000` أو باستخدام متغير البيئة `SKY_ERP_SERVER_URL`. لا تثبّت Server EXE على أجهزة الموظفين.

أضيفت مزامنة خفيفة للاستعلامات النشطة كل ثماني ثوانٍ؛ لذلك تصبح التعديلات المحفوظة على الخادم المركزي ظاهرة تلقائيًا في الشاشات المفتوحة. تظل قاعدة البيانات المركزية هي المصدر الوحيد للبيانات، ولا تُستبدل طريقة التشغيل الحالية all-in-one.
